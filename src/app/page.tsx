import { PhoneFrame } from "@/components/phone-frame";
import { LauncherOS } from "@/components/launcher-os";

export default function Home() {
  return (
    <PhoneFrame>
      <LauncherOS />
    </PhoneFrame>
  );
}
